# src

Place implementation code here.

Suggested structure:
- `src/models/` for model classes or core logic
- `src/utils/` for helper functions
- `src/train.py` or equivalent entry point for experiments

Keep code modular and documented.
